package org.apache.spark.mllib.linalg

/**
 * Created by Fokko on 17-4-15.
 */
object MatrixPub {
/*
  implicit class MatrixPublications(val matrix : Matrix) extends AnyVal {
    def toBreeze : breeze.linalg.Matrix[scala.Vector] = matrix.toBreeze
  }

  implicit class BreezeMatrixPublications(val breezeMatrix : breeze.linalg.Matrix[Vector]) extends AnyVal {
    def fromBreeze : Matrix = Matrices.fromBreeze(breezeMatrix)
  }*/
}
